package com.example;

public class GhostHome extends EmptyBlock {

	/**
	 * Constructor for GhostHome class.
	 * Since GhostHome is an EmptyBlock, it can just extend the EmptyBlock class
	 * 
	 * @param x The x-coordinate of the ghost home.
	 * @param y The y-coordinate of the ghost home.
	 */
	public GhostHome(int x, int y) {
		super(x,y);
	}
	
}
