package com.example;

import javafx.scene.image.Image;

public class Ghost extends MoveableBlock {

    public Ghost(Image image, int x, int y, String type) {
        super(image, x, y, type);
    }
}
