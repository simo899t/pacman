package com.example;

import javafx.scene.image.Image;

public class Pacman extends MoveableBlock {

    public Pacman(Image image, int x, int y, String type) {
        super(image, x, y, type);
    }
}
