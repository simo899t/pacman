package com.example;

import javafx.scene.input.KeyEvent;

/**
 * IController interface defines the contract for handling key events in the game.
 */
public interface IController {
    void keyPressed(KeyEvent e);
}
